#pragma once    //ifdef...とかしなくても, 現代では，これで行けるらしいな
#include <iostream>
#include <fstream>
#include <vector>
#include <cmath>

class BC_solver {
    public:

    std :: vector<double> b; //bをベクトルにする
    b.resize(N-1); // 0~ N-1までのN個の要素


};
